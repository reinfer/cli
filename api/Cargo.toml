[package]
name = "reinfer-client"
version = "0.11.0"
description = "API client for Re:infer, the conversational data intelligence platform"
homepage = "https://github.com/reinfer/cli"
readme = "README.md"
authors = ["reinfer Ltd. <eng@reinfer.io>"]
license = "Apache-2.0"
edition = "2021"

[lib]
name = "reinfer_client"

[dependencies]
chrono = { version = "0.4.19", features = ["serde"] }
log = "0.4.14"
once_cell = "1.9.0"
ordered-float = { version = "3.0.0", features = ["serde"] }
regex = "1.5.4"
reqwest = { version = "0.11.9", default-features = false, features = ["blocking", "gzip", "json", "multipart", "native-tls-vendored"] }
serde = { version = "1.0.133", features = ["derive"] }
serde_json = "1.0.75"
serde_with = "1.11.0"
thiserror = "1.0.30"
url = "2.2.2"

[dev-dependencies]
mockito = "0.30.0"
